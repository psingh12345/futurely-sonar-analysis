from django.test import TestCase

# Create your tests here.

# html_string = render(request, self.template_name, context).content.decode('utf-8')
# pdf = pisa.CreatePDF(html_string)

# # Return PDF as a response
# if not pdf.err:
#     response = HttpResponse(pdf.dest.getvalue(), content_type='application/pdf')
#     response['Content-Disposition'] = 'attachment; filename="unimercatorum-certificate.pdf"'
#     return response
